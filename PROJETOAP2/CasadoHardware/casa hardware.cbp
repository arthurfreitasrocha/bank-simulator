#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <windows.h>
#include <string.h>

char comando[100], txt[100], conteudo[1000], valor[100];
int resp, i, id, quant=0, item, cont_compra=0;

char palavra1[1000][1000] = {"nome completo", "idade", "cpf", "celular", "endereço", "email", "login", "senha"};
char palavra2[1000][1000] = {"nome completo", "idade", "cpf", "celular", "endereço", "email", "login", "senha"};

char cartao1[1000][1000] = {"ncartao", "data_validade", "ncodigo", "nconta"};
char cartao2[1000][1000] = {"ncartao", "data_validade", "ncodigo", "nconta"};

char cel1[1000][1000] = {"CAPA", "FONE", "CARREGADOR", "CHIP", "CARTAO SD", "PELICULA"};
char cel2[1000][1000] = {"30.00", "30.00", "40.00", "10.00","20.00", "30.00"};

char eletro1[1000][1000] = {"COMPUTADORES", "NOTEBOOKS", "MOUSES", "TECLADOS", "MOUSEPADS", "FONES"};
char eletro2[1000][1000] = {"1500.00", "1500.00", "30.00", "45.00", "8.00", "30.00"};

char hard1[1000][1000] = {"COOLERS", "HD", "FONTES", "MEMÓRIA RAM", "PLACA DE VIDEO", "PLACA MAE", "PROCESSADORES", "SSD"};
char hard2[1000][1000] = {"20.00", "280.00", "44.60", "94.00", "218.00", "352.00", "330.50", "110.00", "160.00"};


//FUNÇÃO INICIAL DO PROGRAMA
int main() // FINALIZADO
{
    system("cls");

    setlocale(LC_ALL, "Portuguese");

    strcpy(comando, "rmdir carrinho /s /q");
    system(comando);

    system("cls");

    del_doc();

    printf("\n\t\tBEM-VINDO A CASA DO HARDWARE!");
    printf("\n               O que você quer fazer primeiro?\n\n");

    printf("[0] LOGIN\n");
    printf("[1] CADASTRO\n");
    printf("[2] ACESSAR CATÁLOGO\n");
    printf("[3] SAIR\n\n");

    printf("OPÇÃO: "); scanf("%d",&resp);
    if(resp == 0)
        login_func();
    if(resp == 1)
        quest();
    if(resp == 2)
        catalogo();
    if(resp == 3)
    {
        system("cls");

        printf("\n\tMUITO OBRIGADO PELA PREFERÊNCIA! ATÉ A PRÓXIMA\n\n"); system("pause");

        //return 0;
    }
    if((resp != 0) && (resp != 1) && (resp != 2) && (resp != 3))
    {
        printf("\n\tRESPOSTA INVÁLIDA!\n\n"); system("pause");

        main();
    }
}

void login_func() // FINALIZADO
{
    system("cls");

    char login[100], senha[100], login_file[100], senha_file[100];

    printf("\n\tREALIZANDO LOGIN...\n\n");

    printf("LOGIN: "); getchar(); gets(login);
    printf("\nSENHA: "); gets(senha);

    strcpy(txt, login); strcat(txt, "\\"); strcat(txt, login); strcat(txt, "_LOGIN.txt");

    FILE *file;

    file = fopen(txt, "r");
    while(fgets(login_file, sizeof(login_file), file) != NULL)
    fclose(file);

    file = fopen(txt, "r");
    while(fgets(senha_file, sizeof(senha_file), file) != NULL)
    fclose(file);

    if((strcmp(login, login_file) != 0) && (strcmp(senha, senha_file) != 0))
    {
        printf("\n\tUM OU MAIS DADOS INVÁLIDOS\n\n"); system("pause");

        main();
    }
    else
    {
        strcpy(palavra2[6], login);

        printf("\n\tLOGADO COM SUCESSO!\n\n"); system("pause");

        catalogo();
    }
}

//FUNÇÃO PARA O USUÁRIO ESCOLHER ENTRE FAZER UMA NOVA CONTA OU USAR OS DADOS DO BANCO 99
void quest() // FINALIZADO
{
    system("cls");

    printf("\n\t\tNÓS SOMOS PARCEIROS DO BANCO 99!\n");
    printf("\n\tVocê já possui conta bancária no Banco 99?\n\n");

    printf("[0] SIM\n");
    printf("[1] NÃO\n\n");

    printf("OPÇÃO: "); scanf("%d",&resp); getchar();

    //POSSUI CONTA NO BANCO
    if(resp == 0)
    {
        system("cls");

        printf("\n\t\tNÓS SOMOS PARCEIROS DO BANCO 99!\n");
        printf("\n\tDeseja realizar seu cadastro utilizando sua conta do Banco 99?\n\n");

        printf("[0] SIM\n");
        printf("[1] NÃO\n\n");

        printf("OPÇÃO: "); scanf("%d",&resp); getchar();

        //CADASTRO COM AS INFORMAÇÕES DO BANCO
        if(resp == 0)
        {
            system("cls");

            char nconta[100], codigo[100], comando[100], temp[100];

            system("cls");

            FILE *file;

            printf("\n\tNÓS SOMOS PARCEIROS DO BANCO 99!\n");
            printf("\nPor favor nos informe os dados abaixo\n\n");

            printf("\nDIGITE SEU Nº DE CONTA: "); gets(nconta); printf("\n\nLIXO VISUAL LOGO ABAIXO\n\n");

            file = fopen("dados.txt", "w");
            fprintf(file, "%s", nconta);
            fclose(file);

            strcpy(comando, "requisicao_dados.py");
            system(comando);

            printf("\n\nCASO O Nº DA CONTA INFORMADO ESTEJA CORRETO, VOCÊ RECEBERÁ UM EMAIL COM UM CÓDIGO DE VALIDAÇÃO EM ALGUNS INSTANTES.\nPOR FAVOR, DIGITE O CÓDIGO AQUI -- CÓDIGO: "); gets(codigo);

            printf("\n\nLIXO VISUAL LOGO ABAIXO\n\n");

            file = fopen("dados2.txt", "w");
            fprintf(file, "%s", codigo);
            fclose(file);

            strcpy(comando, "requisicao_dados2.py");
            system(comando);

            file = fopen("resposta.txt", "r");
            while(fgets(temp, sizeof(temp), file) != NULL)
            fclose(file);

            if(strcmp(temp, "FALSE") == 0)
            {
                system("cls");

                printf("\n\tOPS. ALGO DEU ERRADO. TEM CERTEZA DE QUE VOCÊ DIGITOU O CÓDIGO CORRETAMENTE?\n\n"); system("pause");

                main();
            }
            else
            {
                system("cls");

                printf("\n\tSUCESSO! CÓDIGO CORRETO.\n\n"); system("pause");

                // PEGANDO AS INFORMAÇÕES
                for(i = 0; i < 6; i++)
                {
                    strcpy(txt, palavra1[i]); strcat(txt, ".txt");

                    file = fopen(txt, "r");
                    while(fgets(palavra2[i], sizeof(palavra2[i]), file) != NULL)
                    fclose(file);
                }

                for(i = 0; i < 4; i++)
                {
                    strcpy(txt, cartao1[i]); strcat(txt, ".txt");

                    file = fopen(txt, "r");
                    while(fgets(cartao2[i], sizeof(cartao2[i]), file) != NULL)
                    fclose(file);
                }
            }

            system("cls");

            //VERIFICAR LOGIN
            func();

            printf("\nSENHA: "); gets(palavra2[7]);

            printf("\n\tCONFIRMAR CADASTRO?\n");

            printf("[0] SIM\n");
            printf("[1] NÃO\n\n");

            printf("OPÇÃO: "); scanf("%d",&resp); printf("\n\nAGUARDE...");

            if(resp == 0)
            {
                email();

                strcpy(comando, "enviar_email.py");
                system(comando);

                erro_conexao();

                //DIRETÓRIO DO USUÁRIO
                CreateDirectory(strlwr(palavra2[6]), NULL);

                //DIRETÓRIO DO CARTÃO
                strcpy(txt, palavra2[6]); strcat(txt, "\\cartao");
                CreateDirectory(strlwr(txt), NULL);

                //INFORMAÇÕES PESSOAIS
                for(i = 0; i < 8; i++)
                {
                    strcpy(txt, strlwr(palavra2[6])); strcat(txt, "\\"); strcat(txt, strlwr(palavra2[6])); strcat(txt, "_"); strcat(txt, strupr(palavra1[i])); strcat(txt, ".txt");

                    FILE *file;

                    file = fopen(txt, "w");
                    fprintf(file, "%s", strlwr(palavra2[i]));
                    fclose(file);
                }

                //CARTÃO
                for(i = 0; i < 4; i++)
                {
                    strcpy(txt, strlwr(palavra2[6])); strcat(txt, "\\cartao\\"); strcat(txt, cartao1[i]); strcat(txt, ".txt");

                    FILE *file;

                    file = fopen(txt, "w");
                    fprintf(file, "%s", strlwr(cartao2[i]));
                    fclose(file);
                }

                strcpy(txt, strlwr(palavra2[6])); strcat(txt, "\\"); strcat(txt, strlwr(palavra2[6])); strcat(txt, "_"); strcat(txt, "ID"); strcat(txt, ".txt");

                FILE *file;

                file = fopen("id_geral.txt", "r");
                fscanf(file, "%d",&id);
                fclose(file);

                file = fopen(txt, "w");
                fprintf(file, "%d", id);
                fclose(file);

                id++;

                file = fopen("id_geral.txt", "w");
                fprintf(file, "%d",id);
                fclose(file);

                del_doc();

                printf("\n\n\tCADASTRO REALIZADO COM SUCESSO! SEJA MUITO BEM-VINDO!\n\n"); system("pause");

                catalogo();
            }
            if(resp == 1)
            {
                printf("\n\tCADASTRO CANCELADO\n\n"); system("pause");

                main();
            }
            if((resp != 0) && (resp != 1))
            {
                printf("\n\tRESPOSTA INVÁLIDA!");

                main();
            }
        }
        if(resp == 1)
        {
            cadastro();
        }
        if((resp != 0) && (resp != 1))
        {
            printf("\n\tRESPOSTA INVÁLIDA!");

            main();
        }
    }
    if(resp == 1)
        cadastro();
    if((resp != 0) && (resp != 1))
    {
        printf("\n\tRESPOSTA INVÁLIDA!");

        main();
    }
}

//FUNÇÃO PARA VERIFICAR O USUÁRIO
void func() // FINALIZADO
{
    system("cls");

    printf("\n\tAQUI ESTÃO SEUS DADOS\n");

    for(i = 0; i < 6; i++)
    {
        printf("\n%s: %s", strupr(palavra1[i]), strupr(palavra2[i]));
    }

    printf("\n\n\tPOR FAVOR INFORME SEU LOGIN E SENHA\n");

    printf("\nLOGIN: "); gets(palavra2[6]);

    strcpy(txt, palavra2[6]); strcat(txt, "\\"); strcat(txt, palavra2[6]); strcat(txt, "_NOME COMPLETO.txt");

    FILE *file;

    file = fopen(txt, "r");
    fclose(file);

    if(file != NULL)
    {
        printf("\n\tERRO: Nome de usuário já em uso\n\n"); system("pause");
        func();
    }
}

//FUNÇÃO PARA REALIZAR O CADASTRO DAQUELES QUE NÃO TEM CONTA NO BANCO 99 OU NÃO QUEREM USAR AS INFORMAÇÕES DO BANCO 99
void cadastro() // FINALIZADO
{
    system("cls");

    int i;

    printf("\n\tINFORME OS SEGUINTES DADOS\n");

    for(i=0; i<8; i++)
    {
        //TRATAMENTO DE ERROS

        if(i == 5)
        {
            int y = 0, cont=0;
            char email[1000];

            printf("\n%s: ", strupr(palavra1[i])); gets(palavra2[i]);

            if(strcmp(palavra2[i], "") == 0)
            {
                printf("\n\nATENÇÃO: Espaço vazio detectado, por favor digite suas informações corretamente\n\n"); system("pause");

                cadastro();
            }

            strcpy(email, palavra2[i]); cont=strlen(email);

            for(y = 0; y < strlen(email); y++)
            {
                if(email[y] == '@')
                {
                    y = strlen(email);
                    i++;
                }
                else
                {
                    cont--;
                }

                if(cont == 0)
                {
                    printf("\n\nATENÇÃO: EMAIL inválido!\n\n"); system("pause");

                    cadastro();
                }
            }
        }


        if(i == 3)
        {
            int conteudo=0;

            printf("\n%s: +55", strupr(palavra1[i])); gets(palavra2[i]);

            if(strcmp(palavra2[i], "") == 0)
            {
                printf("\n\nATENÇÃO: Espaço vazio detectado, por favor digite suas informações corretamente\n\n"); system("pause");

                cadastro();
            }

            conteudo=strlen(palavra2[i]);

            if((conteudo <= 10) || (conteudo > 11))
            {
                printf("\n\nATENÇÃO: Número de celular inválido para o Brasil\n\n"); system("pause");

                cadastro();
            }
        }

        if(i == 2)
        {
            int conteudo=0;

            printf("\n%s: ", strupr(palavra1[i])); gets(palavra2[i]);

            if(strcmp(palavra2[i], "") == 0)
            {
                printf("\n\nATENÇÃO: Espaço vazio detectado, por favor digite suas informações corretamente\n\n"); system("pause");

                cadastro();
            }

            conteudo=strlen(palavra2[i]);

            if((conteudo < 11) || (conteudo > 11))
            {
                printf("\n\nATENÇÃO: CPF inválido\n\n"); system("pause");

                cadastro();
            }
        }

        if(i == 1)
        {
            int anosN=0;

            printf("\n%s: ", strupr(palavra1[i])); gets(palavra2[i]); anosN = atoi(palavra2[i]);

            if(strcmp(palavra2[i], "") == 0)
            {
                printf("\n\nATENÇÃO: Espaço vazio detectado, por favor digite suas informações corretamente\n\n"); system("pause");

                cadastro();
            }

            if(anosN < 18)
            {
                printf("\n\nATENÇÃO: Idade inválida\n\n"); system("pause");

                cadastro();
            }
        }

        if(i == 0)
        {
            int y, cont=0;
            char nome[1000], txt[100];

            //PRINTANDO NOME
            printf("\n%s: ", strupr(palavra1[i])); gets(palavra2[i]);

            if(strcmp(palavra2[i], "") == 0)
            {
                printf("\n\nATENÇÃO: Espaço vazio detectado, por favor digite suas informações corretamente\n\n"); system("pause");

                cadastro();
            }


            strcpy(nome, palavra2[i]);

            for(y=0; y<strlen(nome); y++)
            {
                if(nome[y] == ' ')
                    cont++;
            }

            if(cont == 0)
            {
                printf("\n\nATENÇÃO: NOME inválido\n\n"); system("pause");

                cadastro();
            }
        }

        if((i != 0) && (i != 1) && (i != 2) && (i != 3) && (i != 5))
        {
            printf("\n%s: ", strupr(palavra1[i])); gets(palavra2[i]);

            if(strcmp(palavra2[i], "") == 0)
            {
                printf("\n\nATENÇÃO: Espaço vazio detectado, por favor digite suas informações corretamente\n\n"); system("pause");

                cadastro();
            }
        }
    }

    //FIM DIGITAR INFORMAÇÕES

    printf("\n\nDESEJA CONFIRMAR OS DADOS ACIMA? [ 0=SIM / 1=NÃO ]: "); scanf("%d",&resp);

    if(resp == 0)
    {
        printf("\nProcessando... Aguarde");

        email();

        strcpy(comando, "enviar_email.py");
        system(comando);

        erro_conexao();

        //DIRETORIO DO USUÁRIO
        CreateDirectory(palavra2[6], NULL);

        //INFORMAÇÕES PESSOAIS
        for(i=0; i<8; i++)
        {
            strcpy(txt, palavra2[6]); strcat(txt, "\\"); strcat(txt, palavra2[6]); strcat(txt, "_"); strcat(txt, strupr(palavra1[i])); strcat(txt, ".txt");

            FILE *file;

            file = fopen(txt, "w");
            fprintf(file, "%s", palavra2[i]);
            fclose(file);
        }

        printf("\n\n\tCADASTRO REALIZADO COM SUCESSO! SEJA MUITO BEM-VINDO!\n\n"); system("pause");

        catalogo();
    }
    if(resp == 1)
    {
        printf("\n\tCADASTRO CANCELADO!\n\n"); system("pause");

        main();
    }
    if((resp != 0) && (resp != 1))
    {
        printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

        main();
    }
}

//FUNÇÃO QUE MOSTRA O CATÁLOGO DA LOJA
void catalogo() // FINALIZADO
{
    system("cls");

    for(i = 0; i < 8; i++)
    {
        strcpy(txt, palavra2[6]); strcat(txt, "\\"); strcat(txt, palavra2[6]); strcat(txt, "_"); strcat(txt, palavra1[i]); strcat(txt, ".txt");

        FILE *file;

        file = fopen(txt, "r");
        while(fgets(palavra2[i], sizeof(palavra2[i]), file) != NULL)
        fclose(file);
    }

    if(strcmp(palavra2[0], "nome completo") == 0)
    {
        strcpy(palavra2[0], "cliente");
    }

    printf("\n\tOLÁ |%s| E SEJA MUITO BEM-VINDO A CASA DO HARDWARE!\n", palavra2[0]);
    printf("\nO que você quer olhar primeiro?\n\n");

    printf("[0] ITENS PARA CELULAR\n");
    printf("[1] ELETRÔNICOS\n");
    printf("[2] HARDWARE\n");
    printf("[3] CARRINHO\n");
    printf("[4] SAIR\n\n");

    printf("OPÇÃO: "); scanf("%d",&resp);

    if(resp == 0)
        celular();
    if(resp == 1)
        eletronicos();
    if(resp == 2)
        hardware();
    if(resp == 3)
        menu_compra();
    if(resp == 4)
    {
        system("cls");

        printf("\n\tDESEJA SAIR OU VOLTAR AO MENU PRINCIPAL?\n\n");

        printf("[0] SAIR\n");
        printf("[1] MENU PRINCIPAL\n\n");

        printf("OPÇÃO: "); scanf("%d",&resp);

        if(resp == 1)
            main();
        if(resp == 0)
        {
            system("cls");

            printf("\n\tMUITO OBRIGADO PELA PREFERÊNCIA! ATÉ A PRÓXIMA\n\n"); system("pause");

            return 0;
        }
    }
    if((resp != 0) && (resp != 1) && (resp != 2) && (resp != 3) && (resp != 4))
    {
        printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

        catalogo();
    }
}

// FUNÇÃO QUE MOSTRA OS ITENS REALACIONADOS A 'ITENS DE CELULAR' QUE ESTÃO A VENDA
void celular() // FINALIZADO
{
    system("cls");

    printf("\n\tBOM |%s|, POR AQUI NÓS TEMOS...\n", palavra2[0]);

    for(i = 0; i < 6; i++)
    {
        printf("\n[%d] %s -> R$: %s", i, cel1[i], cel2[i]);
    }

    printf("\n\n\tQUAL DE NOSSO MARAVILHOS ITENS VOCÊ DESEJA |%s|?\n\n", palavra2[0]);

    printf("OPÇÃO: "); scanf("%d",&item);

    if((resp != 0) && (resp != 1) && (resp != 2) && (resp != 3) && (resp != 4) && (resp != 5))
    {
        printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

        celular();
    }


    printf("\n\tQUANTOS |%s| VOCÊ DESEJA?\n\n", cel1[item]);

    printf("RESPOSTA: "); scanf("%d",&quant);

    //ADICIONANDO PRODUTO ESCOLHIDO AO CARRINHO
    FILE *file;

    strcpy(txt, "carrinho"); strcat(txt, "\\carrinho.txt");

    file = fopen(txt, "r");
    fclose(file);

    if(file == NULL)
    {
        //CRIANDO DIRETÓRIO DO CARRINHO E 'carrinho.txt'
        strcpy(txt, "carrinho");
        CreateDirectory(txt, NULL);

        strcpy(txt, "carrinho\\carrinho.txt");

        file = fopen(txt, "w");
        fclose(file);

        //VERIFICANDO A EXISTÊNCIA DO PRODUTO
        strcpy(txt, "carrinho"); strcat(txt, "\\"); strcat(txt, cel1[item]); strcat(txt, ".txt");

        file = fopen(txt, "r");
        while(fgets(conteudo, sizeof(conteudo), file) != NULL)
        fclose(file);

        //PRODUTO ADICIONADO AO CARRINHO
        strcpy(txt, "carrinho");  strcat(txt, "\\"); strcat(txt, cel1[item]); strcat(txt, ".txt");

        file = fopen(txt, "w");
        fprintf(file, "ITEM: %s --- QUANT.: %d --- VALOR: R$%s\n", cel1[item], quant, cel2[item]);
        fclose(file);

        printf("\n\t|%s| ADICIONADO AO CARRINHO COM SUCESSO!\n\n", cel1[item]); system("pause");

        //CONTADOR DE COMPRAS
        cont_compra++;

        strcpy(txt, "carrinho");  strcat(txt, "\\contador.txt");

        file = fopen(txt, "w");
        fprintf(file, "%d", cont_compra);
        fclose(file);
    }
    else
    {
        //VERIFICANDO A EXISTÊNCIA DO PRODUTO
        strcpy(txt, "carrinho"); strcat(txt, "\\"); strcat(txt, cel1[item]); strcat(txt, ".txt");

        file = fopen(txt, "r");
        while(fgets(conteudo, sizeof(conteudo), file) != NULL)
        fclose(file);

        if(file == NULL)
        {
            //PRODUTO ADICIONADO AO CARRINHO
            strcpy(txt, "carrinho");  strcat(txt, "\\"); strcat(txt, cel1[item]); strcat(txt, ".txt");

            file = fopen(txt, "w");
            fprintf(file, "ITEM: %s --- QUANT.: %d --- VALOR: R$%s\n", cel1[item], quant, cel2[item]);
            fclose(file);

            printf("\n\t|%s| ADICIONADO AO CARRINHO COM SUCESSO!\n\n", cel1[item]); system("pause");
        }
        else
        {
            printf("\n\tPARECE QUE VOCÊ JÁ TEM |%s| ADICIONADO. DESEJA SOBREESCRÊVE-LO?\n\n", cel1[item]);

            printf("[0] SIM\n");
            printf("[1] NÃO\n\n");

            printf("OPÇÃO: "); scanf("%d",&resp);

            if(resp == 0)
            {
                //PRODUTO ADICIONADO AO CARRINHO
                strcpy(txt, "carrinho");  strcat(txt, "\\"); strcat(txt, cel1[item]); strcat(txt, ".txt");

                file = fopen(txt, "w");
                fprintf(file, "ITEM: %s --- QUANT.: %d --- VALOR: R$%s\n", cel1[item], quant, cel2[item]);
                fclose(file);

                printf("\n\t|%s| ADICIONADO AO CARRINHO COM SUCESSO!\n\n", cel1[item]); system("pause");
            }
            if(resp == 1)
            {
                printf("\n\t|%s| NÃO ADICIONADO AO CARRINHO\n\n", cel1[item]); system("pause");
            }
            if((resp != 0) && (resp != 1))
            {
                printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

                catalogo();
            }

        }

        //CONTADOR DE COMPRAS
        cont_compra++;

        strcpy(txt, "carrinho");  strcat(txt, "\\contador.txt");

        file = fopen(txt, "w");
        fprintf(file, "%d", cont_compra);
        fclose(file);
    }

    system("cls");

    printf("\n\tOK! ESTAMOS QUASE FINALIZANDO SUA COMPRA. MAS ANTES NOS DIGA UMA COISA.\nVOCÊ QUER DAR UMA OLHADA EM OUTROS PRODUTOS OU FINALIZAR SUA COMPRA?\n\n");

    printf("[0] DAR UMA OLHADA EM OUTROS PRODUTOS\n");
    printf("[1] FINALIZAR COMPRA\n\n");

    printf("OPÇÃO: "); scanf("%d",&resp);

    if(resp == 0)
    {
        catalogo();
    }
    if(resp == 1)
    {
        menu_compra();
    }
    if((resp != -1) || (resp != -2))
    {
        printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

        celular();
    }
}

// FUNÇÃO QUE MOSTRA OS ITENS REALACIONADOS A 'ELETRÔNICOS' QUE ESTÃO A VENDA
void eletronicos() // FINALIZADO
{
    system("cls");

    printf("\n\tBOM |%s|, POR AQUI NÓS TEMOS...\n", palavra2[0]);

    for(i = 0; i < 6; i++)
    {
        printf("\n[%d] %s -> R$: %s", i, eletro1[i], eletro2[i]);
    }

    printf("\n\n\tQUAL DE NOSSO MARAVILHOS ITENS VOCÊ DESEJA |%s|?\n\n", palavra2[0]);

    printf("OPÇÃO: "); scanf("%d",&item);

    if((resp != 0) && (resp != 1) && (resp != 2) && (resp != 3) && (resp != 4) && (resp != 5))
    {
        printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

        celular();
    }


    printf("\n\tQUANTOS |%s| VOCÊ DESEJA?\n\n", eletro1[item]);

    printf("RESPOSTA: "); scanf("%d",&quant);

    //ADICIONANDO PRODUTO ESCOLHIDO AO CARRINHO
    FILE *file;

    strcpy(txt, "carrinho"); strcat(txt, "\\carrinho.txt");

    file = fopen(txt, "r");
    fclose(file);

    if(file == NULL)
    {
        //CRIANDO DIRETÓRIO DO CARRINHO E 'carrinho.txt'
        strcpy(txt, "carrinho");
        CreateDirectory(txt, NULL);

        strcpy(txt, "carrinho\\carrinho.txt");

        file = fopen(txt, "w");
        fclose(file);

        //VERIFICANDO A EXISTÊNCIA DO PRODUTO
        strcpy(txt, "carrinho"); strcat(txt, "\\"); strcat(txt, eletro1[item]); strcat(txt, ".txt");

        file = fopen(txt, "r");
        while(fgets(conteudo, sizeof(conteudo), file) != NULL)
        fclose(file);

        //PRODUTO ADICIONADO AO CARRINHO
        strcpy(txt, "carrinho");  strcat(txt, "\\"); strcat(txt, eletro1[item]); strcat(txt, ".txt");

        file = fopen(txt, "w");
        fprintf(file, "ITEM: %s --- QUANT.: %d --- VALOR: R$%s\n", eletro1[item], quant, eletro2[item]);
        fclose(file);

        printf("\n\t|%s| ADICIONADO AO CARRINHO COM SUCESSO!\n\n", eletro1[item]); system("pause");

        //CONTADOR DE COMPRAS
        cont_compra++;

        strcpy(txt, "carrinho");  strcat(txt, "\\contador.txt");

        file = fopen(txt, "w");
        fprintf(file, "%d", cont_compra);
        fclose(file);
    }
    else
    {
        //VERIFICANDO A EXISTÊNCIA DO PRODUTO
        strcpy(txt, "carrinho"); strcat(txt, "\\"); strcat(txt, eletro1[item]); strcat(txt, ".txt");

        file = fopen(txt, "r");
        while(fgets(conteudo, sizeof(conteudo), file) != NULL)
        fclose(file);

        if(file == NULL)
        {
            //PRODUTO ADICIONADO AO CARRINHO
            strcpy(txt, "carrinho");  strcat(txt, "\\"); strcat(txt, eletro1[item]); strcat(txt, ".txt");

            file = fopen(txt, "w");
            fprintf(file, "ITEM: %s --- QUANT.: %d --- VALOR: R$%s\n", eletro1[item], quant, eletro2[item]);
            fclose(file);

            printf("\n\t|%s| ADICIONADO AO CARRINHO COM SUCESSO!\n\n", eletro1[item]); system("pause");
        }
        else
        {
            printf("\n\tPARECE QUE VOCÊ JÁ TEM |%s| ADICIONADO. DESEJA SOBREESCRÊVE-LO?\n\n", eletro1[item]);

            printf("[0] SIM\n");
            printf("[1] NÃO\n\n");

            printf("OPÇÃO: "); scanf("%d",&resp);

            if(resp == 0)
            {
                //PRODUTO ADICIONADO AO CARRINHO
                strcpy(txt, "carrinho");  strcat(txt, "\\"); strcat(txt, eletro1[item]); strcat(txt, ".txt");

                file = fopen(txt, "w");
                fprintf(file, "ITEM: %s --- QUANT.: %d --- VALOR: R$%s\n", eletro1[item], quant, eletro2[item]);
                fclose(file);

                printf("\n\t|%s| ADICIONADO AO CARRINHO COM SUCESSO!\n\n", eletro1[item]); system("pause");
            }
            if(resp == 1)
            {
                printf("\n\t|%s| NÃO ADICIONADO AO CARRINHO\n\n", eletro1[item]); system("pause");
            }
            if((resp != 0) && (resp != 1))
            {
                printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

                catalogo();
            }

        }

        //CONTADOR DE COMPRAS
        cont_compra++;

        strcpy(txt, "carrinho");  strcat(txt, "\\contador.txt");

        file = fopen(txt, "w");
        fprintf(file, "%d", cont_compra);
        fclose(file);
    }

    system("cls");

    printf("\n\tOK! ESTAMOS QUASE FINALIZANDO SUA COMPRA. MAS ANTES NOS DIGA UMA COISA.\nVOCÊ QUER DAR UMA OLHADA EM OUTROS PRODUTOS OU FINALIZAR SUA COMPRA?\n\n");

    printf("[0] DAR UMA OLHADA EM OUTROS PRODUTOS\n");
    printf("[1] FINALIZAR COMPRA\n\n");

    printf("OPÇÃO: "); scanf("%d",&resp);

    if(resp == 0)
    {
        catalogo();
    }
    if(resp == 1)
    {
        menu_compra();
    }
    if((resp != -1) || (resp != -2))
    {
        printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

        hardware();
    }
}

// FUNÇÃO QUE MOSTRA OS ITENS REALACIONADOS A 'HARDWARE' QUE ESTÃO A VENDA
void hardware() // FINALIZADO
{
    system("cls");

    printf("\n\tBOM |%s|, POR AQUI NÓS TEMOS...\n", palavra2[0]);

    for(i = 0; i < 8; i++)
    {
        printf("\n[%d] %s -> R$: %s", i, hard1[i], hard2[i]);
    }

    printf("\n\n\tQUAL DE NOSSO MARAVILHOS ITENS VOCÊ DESEJA |%s|?\n\n", palavra2[0]);

    printf("OPÇÃO: "); scanf("%d",&item);

    if((resp != 0) && (resp != 1) && (resp != 2) && (resp != 3) && (resp != 4) && (resp != 5) && (resp != 6) && (resp != 7))
    {
        printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

        celular();
    }


    printf("\n\tQUANTOS |%s| VOCÊ DESEJA?\n\n", hard1[item]);

    printf("RESPOSTA: "); scanf("%d",&quant);

    //ADICIONANDO PRODUTO ESCOLHIDO AO CARRINHO
    FILE *file;

    strcpy(txt, "carrinho"); strcat(txt, "\\carrinho.txt");

    file = fopen(txt, "r");
    fclose(file);

    if(file == NULL)
    {
        //CRIANDO DIRETÓRIO DO CARRINHO E 'carrinho.txt'
        strcpy(txt, "carrinho");
        CreateDirectory(txt, NULL);

        strcpy(txt, "carrinho\\carrinho.txt");

        file = fopen(txt, "w");
        fclose(file);

        //VERIFICANDO A EXISTÊNCIA DO PRODUTO
        strcpy(txt, "carrinho"); strcat(txt, "\\"); strcat(txt, hard1[item]); strcat(txt, ".txt");

        file = fopen(txt, "r");
        while(fgets(conteudo, sizeof(conteudo), file) != NULL)
        fclose(file);

        //PRODUTO ADICIONADO AO CARRINHO
        strcpy(txt, "carrinho");  strcat(txt, "\\"); strcat(txt, hard1[item]); strcat(txt, ".txt");

        file = fopen(txt, "w");
        fprintf(file, "ITEM: %s --- QUANT.: %d --- VALOR: R$%s\n", hard1[item], quant, hard2[item]);
        fclose(file);

        printf("\n\t|%s| ADICIONADO AO CARRINHO COM SUCESSO!\n\n", hard1[item]); system("pause");

        //CONTADOR DE COMPRAS
        cont_compra++;

        strcpy(txt, "carrinho");  strcat(txt, "\\contador.txt");

        file = fopen(txt, "w");
        fprintf(file, "%d", cont_compra);
        fclose(file);
    }
    else
    {
        //VERIFICANDO A EXISTÊNCIA DO PRODUTO
        strcpy(txt, "carrinho"); strcat(txt, "\\"); strcat(txt, hard1[item]); strcat(txt, ".txt");

        file = fopen(txt, "r");
        while(fgets(conteudo, sizeof(conteudo), file) != NULL)
        fclose(file);

        if(file == NULL)
        {
            //PRODUTO ADICIONADO AO CARRINHO
            strcpy(txt, "carrinho");  strcat(txt, "\\"); strcat(txt, hard1[item]); strcat(txt, ".txt");

            file = fopen(txt, "w");
            fprintf(file, "ITEM: %s --- QUANT.: %d --- VALOR: R$%s\n", hard1[item], quant, hard2[item]);
            fclose(file);

            printf("\n\t|%s| ADICIONADO AO CARRINHO COM SUCESSO!\n\n", hard1[item]); system("pause");
        }
        else
        {
            printf("\n\tPARECE QUE VOCÊ JÁ TEM |%s| ADICIONADO. DESEJA SOBREESCRÊVE-LO?\n\n", hard1[item]);

            printf("[0] SIM\n");
            printf("[1] NÃO\n\n");

            printf("OPÇÃO: "); scanf("%d",&resp);

            if(resp == 0)
            {
                //PRODUTO ADICIONADO AO CARRINHO
                strcpy(txt, "carrinho");  strcat(txt, "\\"); strcat(txt, hard1[item]); strcat(txt, ".txt");

                file = fopen(txt, "w");
                fprintf(file, "ITEM: %s --- QUANT.: %d --- VALOR: R$%s\n", hard1[item], quant, hard2[item]);
                fclose(file);

                printf("\n\t|%s| ADICIONADO AO CARRINHO COM SUCESSO!\n\n", hard1[item]); system("pause");
            }
            if(resp == 1)
            {
                printf("\n\t|%s| NÃO ADICIONADO AO CARRINHO\n\n", hard1[item]); system("pause");
            }
            if((resp != 0) && (resp != 1))
            {
                printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

                catalogo();
            }

        }

        //CONTADOR DE COMPRAS
        cont_compra++;

        strcpy(txt, "carrinho");  strcat(txt, "\\contador.txt");

        file = fopen(txt, "w");
        fprintf(file, "%d", cont_compra);
        fclose(file);
    }

    system("cls");

    printf("\n\tOK! ESTAMOS QUASE FINALIZANDO SUA COMPRA. MAS ANTES NOS DIGA UMA COISA.\nVOCÊ QUER DAR UMA OLHADA EM OUTROS PRODUTOS OU FINALIZAR SUA COMPRA?\n\n");

    printf("[0] DAR UMA OLHADA EM OUTROS PRODUTOS\n");
    printf("[1] FINALIZAR COMPRA\n\n");

    printf("OPÇÃO: "); scanf("%d",&resp);

    if(resp == 0)
    {
        catalogo();
    }
    if(resp == 1)
    {
        menu_compra();
    }
    if((resp != -1) || (resp != -2))
    {
        printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

        hardware();
    }
}

// FUNÇÃO QUE EFETUA A COMPRA DOS PRODUTOS
void menu_compra() // FINALIZADO
{
    system("cls");

    char temp[100];
    FILE *file;

    strcpy(txt, palavra2[6]); strcat(txt, "\\"); strcat(txt, palavra2[6]); strcat(txt, "_NOME COMPLETO.txt");
    strcpy(temp, palavra2[0]);

    file = fopen(txt, "r");
    while(fgets(conteudo, sizeof(conteudo), file) != NULL)
    fclose(file);

    if(strcmp(temp, conteudo) != 0)
    {
        system("cls");

        printf("\n\tPARA COMPRAR OS ITENS DE NOSSA LOJA VOCÊ PRECISA ESTAR LOGADO. DEJESA LOGAR-SE OU CRIAR UMA CONTA?\n\n");

        printf("[0] LOGAR\n");
        printf("[1] CRIAR CONTA\n\n");

        printf("OPÇÃO: "); scanf("%d",&resp);

        if(resp == 0)
            login_func();
        if(resp == 1)
            quest();
        if((resp != 0) && (resp != 1))
        {
            printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

            catalogo();
        }
    }

    system("cls");

    strcpy(txt, "carrinho\\carrinho.txt");

    file = fopen(txt, "r");
    fclose(file);

    if(file == NULL)
    {
        printf("\n\tOPS. PARECE QUE SEU CARRINHO ESTÁ VAZIO\n\n"); system("pause");

        catalogo();
    }
    else
    {
        printf("\n\tVOCÊ FEZ EXCELENTES ESCOLHAS |%s|! OBRIGADO POR SELECIONAR:\n\n", palavra2[0]);

        //EXECUTA VARREDURA NO DIRETÓRIO DO CARRINHO
        strcpy(comando, "varredura.bat");
        system(comando);

        file = fopen("resp_varredura.txt", "r");
        while(fgets(conteudo, sizeof(conteudo), file) != NULL)
            printf("%s", conteudo);
        fclose(file);

        //EXECUTA VARREDURA DO PREÇO TOTAL
        strcpy(comando, "varredura_preco.bat");
        system(comando);

        file = fopen("resposta_preco.txt", "r");
        while(fgets(valor, sizeof(valor), file) != NULL)
        fclose(file);

        printf("\nTOTAL: R$%s", valor);

        printf("\n\n\tCONFIRMAR COMPRA OU CANCELAR ITEM?\n\n");

        printf("[0] CONFIRMAR COMPRA\n");
        printf("[1] CANCELAR ITEM\n");
        printf("[2] VOLTAR\n\n");

        printf("OPÇÃO: "); scanf("%d",&resp);

        if(resp == 0)
        {
            strcpy(txt, palavra2[6]); strcat(txt, "\\cartao\\ncartao.txt");

            file = fopen(txt, "r");
            while(fgets(conteudo, sizeof(conteudo), file) != NULL)
            fclose(file);

            if(file == NULL)
            {
                system("cls");

                printf("\n\tVOCÊ NÃO POSSUI NENHUM CARTÃO REGISTRADO. REGISTRAR UM AGORA?\n\n");

                printf("[0] SIM\n");
                printf("[1] NÃO\n\n");

                printf("OPÇÃO: "); scanf("%d",&resp);

                if(resp == 0)
                {
                    registrar_cartao();

                    menu_compra();
                }
                if(resp == 1)
                {
                    catalogo();
                }
                if((resp != 0) && (resp != 1))
                {
                    printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

                    menu_compra();
                }
            }
            else
            {
                system("cls");

                for(i = 0; i < 4; i++)
                {
                    strcpy(txt, palavra2[6]); strcat(txt, "\\cartao\\"); strcat(txt, cartao1[i]); strcat(txt, ".txt");

                    FILE *file;

                    file = fopen(txt, "r");
                    while(fgets(cartao2[i], sizeof(cartao2[i]), file) != NULL)
                    fclose(file);
                }

                char temp[100][100];

                printf("\n\tPAGAR COM CARTÃO MASTERCARD BANCO 99?:\n\n");

                for(i = 0; i < strlen(cartao2[0]); i++)
                {
                    temp[0][i] = cartao2[0][i];

                    if(temp[0][i] == ' ')
                        i = strlen(cartao2[0]);
                }

                printf("Nº CARTÃO: %s**** **** ****\n\n", temp);

                printf("[0] SIM\n");
                printf("[1] NÃO\n\n");

                printf("OPÇÃO: "); scanf("%d",&resp);

                if(resp == 0)
                {
                    system("cls");

                    printf("\n\tVOCÊ GOSTARIA DE PAGAR NO CRÉDITO OU NO DÉBITO?\n\n");

                    printf("[0] CRÉDITO\n");
                    printf("[1] DÉBITO\n\n");

                    printf("OPÇÃO: "); scanf("%d",&resp); printf("\n\nLIXO VISUAL LOGO ABAIXO\n\n");

                    if(resp == 0)
                    {
                        file = fopen("info.txt", "w");
                        fprintf(file, "%s-%s-credito", cartao2[3], valor);
                        fclose(file);

                        strcpy(comando, "enviar_info.py");
                        system(comando);

                        file = fopen("resposta.txt", "r");
                        while(fgets(conteudo, sizeof(conteudo), file) != NULL)
                        fclose(file);

                        if(strcmp(conteudo, "FALSE") == 0)
                        {
                            system("cls");

                            printf("\n\tOPS. PARECE QUE ALGO DEU ERRADO POR AQUI... OCORREU UM FALHA NA OPERAÇÃO\n\n"); system("pause");


                            catalogo();
                        }
                        if(strcmp(conteudo, "TRUE") == 0)
                        {
                            system("cls");

                            strcpy(comando, "rmdir carrinho /s /q");
                            system(comando);

                            printf("\n\tCOMPRA REALIZADA COM SUCESSO!\n\n"); system("pause");

                            catalogo();
                        }
                    }
                    if(resp == 1)
                    {
                        file = fopen("info.txt", "w");
                        fprintf(file, "%s-%s-debito", cartao2[3], valor);
                        fclose(file);

                        strcpy(comando, "enviar_info.py");
                        system(comando);

                        file = fopen("resposta.txt", "r");
                        while(fgets(conteudo, sizeof(conteudo), file) != NULL)
                        fclose(file);

                        if(strcmp(conteudo, "FALSE") == 0)
                        {
                            system("cls");

                            printf("\n\tOPS. PARECE QUE ALGO DEU ERRADO POR AQUI... OCORREU UM FALHA NA OPERAÇÃO\n\n"); system("pause");

                            catalogo();
                        }
                        if(strcmp(conteudo, "TRUE") == 0)
                        {
                            system("cls");

                            strcpy(comando, "rmdir carrinho /s /q");
                            system(comando);

                            printf("\n\tCOMPRA REALIZADA COM SUCESSO!\n\n"); system("pause");

                            catalogo();
                        }
                    }
                    if((resp != 0) && (resp != 1))
                    {
                        printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

                        menu_compra();
                    }
                }
                if(resp == 1)
                {
                    printf("\n\tDESCULPE, MAS NO MOMENTO NÓS ACEITAMOS APENAS CARTÕES MASTERCARD BANCO 99\n\n"); system("pause");

                    menu_compra();
                }
                if((resp != 0) && (resp != 1))
                {
                    printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

                    menu_compra();
                }

            }
        }
        if(resp == 1)
        {
            int indice=0, item;

            file = fopen("resp_varredura_indice.txt", "r");
            fscanf(file, "%d",&indice);
            fclose(file);

            printf("\n\n\tQUAL ITEM VOCÊ DESEJA CANCELAR?\n\n");

            printf("OPÇÃO: "); scanf("%d",&item);

            if((resp < 0) || (resp > indice))
            {
                printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

                system("cls"); menu_compra();
            }
            else
            {
                file = fopen("resp_varredura_indice.txt", "w");
                fprintf(file, "%d", item);
                fclose(file);

                strcpy(comando, "varredura_del.bat");
                system(comando);

                file = fopen("resp_varredura_item.txt", "r");
                while(fgets(conteudo, sizeof(conteudo), file) != NULL)
                fclose(file);

                printf("\n\tSUCESSO! %s\t\tRETIRADO DO CARRINHO!\n\n", conteudo); system("pause");

                menu_compra();
            }
        }
        if(resp == 2)
        {
            catalogo();
        }
        if((resp != 0) && (resp != 1) && (resp != 2))
        {
            printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

            menu_compra();
        }
    }
}

//FUNÇÃO PARA REGISTRAR O CARTÃO
void registrar_cartao() // FINALIZADO
{
    cartao:
    system("cls");

    char nconta[100];

    printf("\n\tNÓS ACEITAMOS APENAS CARTÕES DO BANCO 99. POIS OS MESMOS SÃO NOSSOS PARCEIROS.\n\t\tPOR FAVOR, DIGITE AS INFORMAÇÕES ABAIXO\n\n");

    printf("Nº CONTA: "); getchar(); gets(nconta);

    for(i = 0; i < 3; i++)
    {
        printf("\n%s: ", strupr(cartao1[i])); gets(cartao2[i]);
    }

    printf("\n\tSEUS DADOS ESTÃO CORRETOS?\n\n");

    printf("[0] SIM\n");
    printf("[1] NÃO\n\n");

    printf("OPÇÃO: "); scanf("%d",&resp); printf("\n\nLIXO VISUAL LOGO ABAIXO\n\n");

    if(resp == 0)
    {
        FILE *file;

        file = fopen("cartao.txt", "w");
        fprintf(file, "%s-%s-%s-%s", nconta, cartao2[0], cartao2[1], cartao2[2]);
        fclose(file);

        strcpy(comando, "requisicao_cartao.py");
        system(comando);

        file = fopen("resposta.txt", "r");
        while(fgets(conteudo, sizeof(conteudo), file) != NULL)
        fclose(file);

        if(strcmp(conteudo, "FALSE") == 0)
        {
            system("cls");

            printf("\n\tOPS. PARECE QUE ALGO DEU ERRADO. TEM CERTEZA DE QUE VOCÊ DIGITOU SUAS INFORMAÇÕES CORRETAMENTE?\n\n"); system("pause");

            goto cartao;
        }
        else
        {
            //DIRETÓRIO DO CARTÃO
            strcpy(txt, palavra2[6]); strcat(txt, "\\cartao");
            CreateDirectory(strlwr(txt), NULL);

            //CARTÃO
            for(i = 0; i < 4; i++)
            {
                strcpy(comando, "move "); strcat(comando, cartao1[i]); strcat(comando, ".txt"); strcat(comando, " "); strcat(comando, palavra2[6]); strcat(comando, "\\cartao");
                system(comando);
            }

            system("cls");

            printf("\n\tSUCESSO! CARTÃO ADICIONADO\n\n"); system("pause");
        }
    }
    if(resp == 1)
    {
        goto cartao;
    }
    if((resp != 0) && (resp != 1))
    {
        printf("\n\tOPÇÃO INVÁLIDA\n\n"); system("pause");

        registrar_cartao();
    }
}

//FUNÇÃO PARA INFORMAR O EMAIL DO USUÁRIO EM UM TXT
void email() // FINALIZADO
{
    char txt[1000];

    strcpy(txt, palavra2[6]); strcat(txt, "\\"); strcat(txt, palavra2[6]); strcat(txt, "_"); strcat(txt, palavra1[5]); strcat(txt, ".txt");

    FILE *arq;
    arq = fopen(txt, "r");
    fscanf(arq, "%s", palavra2[5]);
    fclose(arq);

    arq = fopen("1.txt", "w");
    fprintf(arq, "%s", palavra2[5]);
    fclose(arq);
}

//FUNÇÃO QUE AVISA QUANDO NÃO HÁ CONEXÃO COM A INTERNET
void erro_conexao() // FINALIZADO
{
    char conteudo[1000];

    FILE *documento;
    documento = fopen("2.txt", "r");

    if(documento == NULL)
        fclose(documento);
    else
    {
        while(fgets(conteudo, sizeof(conteudo), documento) != NULL); printf("\n\n\t");
        printf("%s", conteudo); printf("\n\n"); system("pause");

        del_doc();

        fclose(documento);

        main();
    }
}

//FUNÇÃO QUE APAGA OS DOCUMENTOS QUE SE TORNAM DESNECESSÁRIOS APÓS SEREM USADOS
void del_doc() //FINALIZADO
{
    remove("0.txt");
    remove("1.txt");
    remove("2.txt");
    remove("3.txt");
    remove("4.txt");
    remove("nome completo.txt");
    remove("idade.txt");
    remove("cpf.txt");
    remove("email.txt");
    remove("endereço.txt");
    remove("celular.txt");
    remove("dados.txt");
    remove("resposta.txt");
    remove("data_validade.txt");
    remove("ncartao.txt");
    remove("ncodigo.txt");
    remove("cartao.txt");
    remove("resp_varredura.txt");
    remove("resposta_preco.txt");
    remove("varredura.txt");
    remove("nconta.txt");
    remove("info.txt");
    remove("dados2.txt");
    remove("resp_varredura_indice.txt");
    remove("resp_varredura_item.txt");
}
